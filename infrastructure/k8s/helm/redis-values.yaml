# helm install redis bitnami/redis -f redis-values.yaml -n notiflow-services
architecture: standalone

auth:
  enabled: false

master:
  persistence:
    enabled: true
    size: 2Gi
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mizx